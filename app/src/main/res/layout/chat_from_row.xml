<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <TextView
        android:id="@+id/chatLog_messageFromRowTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:background="@drawable/rounded_edittext_register_login"
        android:maxWidth="240dp"
        android:padding="16dp"
        android:text="This is my message that is very looooooong"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@+id/chatLog_profileFromRowImageView"
        app:layout_constraintTop_toTopOf="@+id/chatLog_profileFromRowImageView" />

    <ImageView
        android:id="@+id/chatLog_imageFromRowTextView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginEnd="8dp"
        android:adjustViewBounds="true"
        android:background="@drawable/rounded_edittext_register_login"
        android:padding="15dp"
        android:visibility="visible"
        android:scaleType="fitCenter"
        app:layout_constraintStart_toEndOf="@+id/chatLog_profileFromRowImageView"
        app:layout_constraintTop_toTopOf="@+id/chatLog_profileFromRowImageView"
        app:srcCompat="@drawable/ic_image_black" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/chatLog_profileFromRowImageView"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:src="@drawable/ic_baseline_person_24"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>